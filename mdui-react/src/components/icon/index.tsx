import { createComponent } from "@lit/react";
import React from "react";
import { Icon as _MduiIcon } from "mdui/components/icon.js";

export const Icon = createComponent({
  react: React,
  tagName: "mdui-icon",
  elementClass: _MduiIcon,
});
